sphinx>=4.3.0,<9            # >=4.3.0 to avoid bullet-point bug, <9 to avoid incompatibility with sphinx-toolbox
sphinx_rtd_theme>=0.5.1     # To avoid bullet-point bug
sphinx-toolbox<4            # To avoid incompatibility with sphinx>=9
sphinx-design               # For better design of documentation, e.g. tabs and grids
sphinx-argparse             # For better formatting of command-line arguments in documentation
sphinx-copybutton<1         # For copy button in code blocks
#moldoc>=3.0.5,<4            # For 3D rendering of molecules in documentation. Latest version fixes bug #2.
DARTassembler               # Add all the DART dependencies because moldoc calls DARTassembler